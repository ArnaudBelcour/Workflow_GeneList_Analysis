language: python
python:
  - "3.4"
# command to install dependencies
install:
  - pip install pandas
  - pip install numpy
  - pip install scipy
# command to run tests
script:
  - python test/enrichmentAnalysis_test.py
